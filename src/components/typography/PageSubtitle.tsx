const PageSubtitle = ({ text }: { text: string }) => (
  <h2 className="w-full max-w-6xl text-left text-xl font-semibold text-gray-900 dark:text-gray-100">
    {text}
  </h2>
);

export default PageSubtitle;
